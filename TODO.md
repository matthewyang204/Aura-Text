# Final cleanup:
- [ ] Move the Notepad== import syspath to a universal importable module that's imported everywhere rather than doing the three dirnames every time
- [ ] Bump the version number before @rohankishore forgets